services:
  privatebin:
    image: privatebin/nginx-fpm-alpine
    container_name: privatebin
    restart: always
    ports:
      - "8088:8080"
    volumes:
      - .:/var/www
      - privatebin_data:/var/www/data

volumes:
  privatebin_data:
